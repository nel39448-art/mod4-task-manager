# Dockerfile multietapa del frontend (React + Vite → nginx)

# Etapa 1: construir la aplicación con Node
FROM node:20-alpine AS build
WORKDIR /app
COPY package*.json ./
COPY prisma ./prisma
RUN npm ci
COPY . .
RUN npm run build

# Etapa 2: servir solo los archivos estáticos con nginx (sin Node en producción)
FROM nginx:alpine
COPY --from=build /app/dist /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
