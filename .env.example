# Variables de entorno del proyecto (copia este archivo a .env y completa los valores)
# Dentro de Docker Compose el host de la base de datos es el nombre del servicio: postgres
DATABASE_URL=postgresql://appuser:apppass@postgres:5432/appdb
PORT=4000
